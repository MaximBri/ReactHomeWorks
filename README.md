# README

## Задание 3

Написан хук `useHover`. **Не принимает никаких параметров**, возвращает объект с **двумя** параметрами:

- `hovered`: наведён ли курсор на элемент
- `ref`: ссылка на сам `HTML` узел

`ref` можно назначить на любой `HTML-тег`, использованы дженерики

---

Не знаю, что ещё написать, вроде всё :(
